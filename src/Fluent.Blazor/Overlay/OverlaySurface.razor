@namespace Fluent.Blazor.Overlay
@implements IAsyncDisposable

<div class="fluent-overlay-surface @(Entry.Bare ? "fluent-overlay-surface--bare" : "")" style="@Entry.ComputedStyle" @ref="_surfaceElement">
    @Entry.Content
</div>
