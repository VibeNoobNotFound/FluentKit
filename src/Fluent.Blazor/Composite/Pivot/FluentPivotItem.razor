@namespace Fluent.Blazor.Composite
@* Renders nothing itself — registers Title/TabTemplate/ChildContent with the parent FluentPivot
   via PivotContext, which owns all actual DOM output. See FluentPivot.razor.cs. *@
