namespace Fluent.Blazor.Composite;

/// <summary>Visual size of a <see cref="FluentContentDialog"/>, mirroring fluent-svelte's
/// ContentDialog `size` prop (min/standard/max) and WinUI's own three dialog width presets.</summary>
public enum ContentDialogSize
{
    Min,
    Standard,
    Max
}
