# FluentKit API reference

> Generated from `FluentKit` version `0.2.4`. Do not edit this file by hand.

## Components

### `FluentAcrylicBrush`

Category: **Effects** · Sample: `/Effects/Acrylic`


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `FallbackColor` | `String` | no | no | no | Overrides the theme-default fallback color shown when the browser has no backdrop-filter support, or under prefers-reduced-transparency. Accepts any valid CSS color. |
| `Kind` | `FluentKit.Effects.AcrylicKind` | no | no | no |  |
| `TintColor` | `String` | no | no | no | Overrides the theme-default tint color (WinUI's AcrylicBrush.TintColor). Accepts any valid CSS color — hex, rgb()/rgba(), a named color, whatever the application already has on hand — since the actual blending is done with color-mix() rather than requiring an r,g,b triplet. |
| `TintLuminosityOpacity` | `Nullable<Double>` | no | no | no | Mirrors WinUI's AcrylicBrush.TintLuminosityOpacity, 0.0-1.0. Real Acrylic composites TWO tint passes: a luminosity-blended wash against the blurred backdrop (keeps the tint reading consistently regardless of how light/dark whatever's actually behind the panel is) UNDER the flat TintOpacity wash above. Left unset, the brush keeps its simpler single-layer tint (the original behavior) — set this to opt into the closer two-layer approximation. |
| `TintOpacity` | `Nullable<Double>` | no | no | no | Overrides the theme-default flat tint wash opacity (WinUI's AcrylicBrush.TintOpacity), 0.0-1.0. This is the single color layer sitting on top of the live blur. |

### `FluentAutoSuggestBox`

Category: **Composite** · Sample: `/Composite/AutoSuggestBox`
Mirrors WinUI's AutoSuggestBox / fluent-svelte's AutoSuggestBox.svelte — a free-typing TextBox with a live-filtered suggestion dropdown underneath, distinct from ComboBox's editable mode in that the typed text is NOT required to match any item: is always whatever the user typed, fires with that raw text on Enter with no suggestion highlighted, and picking a suggestion (click, or Enter while one IS highlighted) fires separately and just replaces Text with the suggestion's Name — there's no persistent "selected value" the way ComboBox has Value. Composes FluentTextBox (same "don't reimplement input chrome" rule NumberBox follows) and, like ComboBox/ContentDialog/NumberBox's Expanded popout, stays self-contained/absolutely positioned rather than going through IOverlayService — the dropdown needs to be exactly the trigger's width, same reasoning as ComboBox's own dropdown.

Generic parameters: `TValue`

| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `Disabled` | `Boolean` | no | no | no |  |
| `EnableSearchIcon` | `Boolean` | no | no | no | When true, shows a search icon on the right of the box (via TextBox's Buttons slot). Once there's text in the box, that icon swaps to a "clear" icon that empties Text on click — same idea as the search field on the demo TextBox above, just wired in as an opt-in here. |
| `Filter` | `Func<String, AutoSuggestBoxItem<TValue>, Boolean>` | no | no | no | Optional custom match predicate over (typed text, candidate item). Defaults to a case-insensitive substring match against — deliberately "contains" rather than ComboBox's "starts-with", since AutoSuggestBox is meant for search-as-you-type against arbitrary positions in the name, not prefix completion. |
| `Header` | `String` | no | no | no |  |
| `ItemTemplate` | `RenderFragment<AutoSuggestBoxItem<TValue>>` | no | no | no | Optional custom row content for each suggestion. Falls back to a plain <span>@item.Name</span>, same default ComboBox's ItemTemplate uses. |
| `Items` | `IReadOnlyList<AutoSuggestBoxItem<TValue>>` | no | no | no |  |
| `MaxSuggestions` | `Int32` | no | no | no | Upper bound on how many matches are shown at once. |
| `Placeholder` | `String` | no | no | no |  |
| `QuerySubmitted` | `EventCallback<String>` | no | no | no | Fired on Enter when no suggestion is highlighted — the raw typed text is the query, same as WinUI's QuerySubmitted with a null ChosenSuggestion. |
| `SuggestionChosen` | `EventCallback<AutoSuggestBoxItem<TValue>>` | no | no | no | Fired when the user picks a suggestion — click, or Enter while one is highlighted via arrow keys. Text is set to the suggestion's Name as part of the same interaction. |
| `Text` | `String` | no | no | yes | The raw typed text. Two-way bindable — always reflects what's in the box, never snapped back to a suggestion's Name unless the user actually picks one. |
| `TextChanged` | `EventCallback<String>` | no | no | no |  |

### `FluentButton`

Category: **Primitives** · Sample: `/Primitives/Button`
Inherits for optional XAML-style Padding/Width/Height/ MinWidth/MinHeight/MaxWidth/MaxHeight overrides — see that class for defaults/semantics.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `Disabled` | `Boolean` | no | no | no |  |
| `Height` | `Nullable<Double>` | no | no | no |  |
| `Href` | `String` | no | no | no | The URL to navigate to. Setting this renders the button as an <a> element instead of a <button> — required for links to actually work (a <button> ignores href entirely). Typically paired with for WinUI HyperlinkButton parity, but Href works with any variant if you want an accent/standard-styled button that's really a link. |
| `MaxHeight` | `Nullable<Double>` | no | no | no |  |
| `MaxWidth` | `Nullable<Double>` | no | no | no |  |
| `MinHeight` | `Nullable<Double>` | no | no | no |  |
| `MinWidth` | `Nullable<Double>` | no | no | no |  |
| `OnClick` | `EventCallback<Microsoft.AspNetCore.Components.Web.MouseEventArgs>` | no | no | no |  |
| `Padding` | `Nullable<FluentKit.Common.Thickness>` | no | no | no | Inner spacing between the border and content, all sides. Unset = the control's CSS default. |
| `Target` | `String` | no | no | no | Anchor "target" attribute. Defaults to "_blank" (opens in a new tab) whenever is set, matching typical hyperlink-button expectations. Set to "_self" to navigate in-place. |
| `Variant` | `FluentKit.Primitives.ButtonVariant` | no | no | no |  |
| `Width` | `Nullable<Double>` | no | no | no |  |

### `FluentCalendarDatePicker`

Category: **Composite** · Sample: `/Composite/Calendar`
Ported from fluent-svelte's CalendarDatePicker.svelte — a button that shows the picked date (or a placeholder) and pops FluentCalendarView open beneath it on click. Per this project's own stated convention (NumberBox's Expanded popout, ComboBox/AutoSuggestBox's dropdown), this is self-contained/absolutely-positioned rather than routed through IOverlayService — fluent-svelte's own version wires CalendarView into its generic Flyout wrapper, but every other "needs exact trigger width/position, not a generic floating panel" composite in this codebase has deliberately skipped the overlay service, and a date picker popout is the same shape of problem. Closing behavior mirrors fluent-svelte: picking a date closes the popout (its CalendarView listens for on:change and sets open = false); Escape closes it; and losing focus entirely closes it. The latter relies on FluentCalendarViewItem's cells (and this component's own header/ pagination buttons) using @onmousedown:preventDefault so clicking inside the popout never actually blurs the trigger button in the first place — without that, every internal focus change would incorrectly bubble a focusout up to this root and close the popout mid-click, the same problem AutoSuggestBox's dropdown solved the same way.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `Blackout` | `IReadOnlyList<DateTime>` | no | no | no |  |
| `Disabled` | `Boolean` | no | no | no |  |
| `Locale` | `String` | no | no | no |  |
| `Max` | `Nullable<DateTime>` | no | no | no |  |
| `Min` | `Nullable<DateTime>` | no | no | no |  |
| `Placeholder` | `String` | no | no | no |  |
| `SelectionMode` | `FluentKit.Composite.CalendarSelectionMode` | no | no | no | Single (default), Multiple, or Range. See . In Multiple/Range modes the picker's label shows a summary (count of dates / "start – end") instead of a single formatted date, and the popout stays open after a pick so the user can keep selecting — it only closes on Escape, an outside click, or (Range only) once the second, completing date is chosen. |
| `Value` | `Nullable<DateTime>` | no | no | yes |  |
| `ValueChanged` | `EventCallback<Nullable<DateTime>>` | no | no | no |  |
| `Values` | `IReadOnlyList<DateTime>` | no | no | yes | Selected dates when is Multiple or Range. Two-way bindable. Ignored (and used instead) when SelectionMode is Single. |
| `ValuesChanged` | `EventCallback<IReadOnlyList<DateTime>>` | no | no | no |  |
| `WeekStart` | `Int32` | no | no | no |  |

### `FluentCalendarView`

Category: **Composite** · Sample: `/Composite/Calendar`
Ported from fluent-svelte's CalendarView.svelte — lets a user browse and pick a date across three drill levels (days/months/years). Supports single-select (), multi-select (), and range-select (also , interpreted as a chronological [start, end] pair) via — svelte's source only ever had a boolean multiple prop; is kept as a back-compat alias for SelectionMode="CalendarSelectionMode.Multiple", and Range is a net-new addition on top of the ported behavior. Animations: fluent-svelte uses Svelte transition directives (fadeScale on view switch, fly on page turn). Blazor has no direct equivalent, so the same visual effect is reproduced with plain CSS keyframe animations in FluentCalendarView.razor.css, re-triggered every render by keying the table/tbody on (View, Page) via @key — changing a @key forces Blazor to tear down and recreate the element, which restarts the CSS animation named by the relevant direction class (fluent-calendar-view-table--anim-up/down, fluent-calendar-view-body--anim-up/down). Remaining deviation: arrow-key navigation now walks off the current page's edge, auto-pages, and refocuses the newly revealed cell (matching svelte) — see OnGridKeyDownAsync.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `Blackout` | `IReadOnlyList<DateTime>` | no | no | no | Dates the user cannot select — rendered with a diagonal strike, matching fluent-svelte's blackout prop. |
| `Change` | `EventCallback<Nullable<DateTime>>` | no | no | no | Mirrors fluent-svelte's on:change — fires whenever a day is picked/unpicked, same payload as ValueChanged. Kept separate so applications can distinguish "value changed because the user just clicked a day" from any other source of a Value update. |
| `Floating` | `Boolean` | no | no | no | INTERNAL USE — applies the floating/flyout-shadow presentation used when embedded in CalendarDatePicker's popout, mirroring fluent-svelte's __floating prop. |
| `Headers` | `Boolean` | no | no | no | Shows a small overline label (month abbreviation / year) on the first day of each month or first month of each year, matching fluent-svelte's headers prop. |
| `Locale` | `String` | no | no | no | Locale used for month/weekday names and header text. Defaults to the current culture if unset, matching fluent-svelte inferring from navigator.language. |
| `Max` | `Nullable<DateTime>` | no | no | no |  |
| `Min` | `Nullable<DateTime>` | no | no | no |  |
| `Multiple` | `Boolean` | no | no | no | Enables multi-date selection, matching fluent-svelte's multiple prop. When true, clicking days toggles membership in instead of setting . Superseded by — kept for back-compat; setting this true without touching is equivalent to SelectionMode="CalendarSelectionMode.Multiple". |
| `SelectionMode` | `FluentKit.Composite.CalendarSelectionMode` | no | no | no | Single (default), Multiple, or Range. Net-new on top of fluent-svelte's original single/multiple-only API. See for what each does. |
| `Value` | `Nullable<DateTime>` | no | no | yes | The currently selected date, or null. Two-way bindable. Ignored when is true. |
| `ValueChanged` | `EventCallback<Nullable<DateTime>>` | no | no | no |  |
| `Values` | `IReadOnlyList<DateTime>` | no | no | yes | The currently selected dates when is Multiple or Range. Two-way bindable. In Range mode this holds at most two entries, always chronologically ordered: [start] while a range is being picked, or [start, end] once complete. |
| `ValuesChange` | `EventCallback<IReadOnlyList<DateTime>>` | no | no | no | Mirrors fluent-svelte's on:change payload for multi-select mode; also used for Range mode, firing after both the start and (later) the completing end click. |
| `ValuesChanged` | `EventCallback<IReadOnlyList<DateTime>>` | no | no | no |  |
| `View` | `FluentKit.Composite.CalendarViewMode` | no | no | yes | The current drill level. Two-way bindable — clicking the header or a month/year cell changes it, same as fluent-svelte letting the user manually change view via header clicks. |
| `ViewChanged` | `EventCallback<FluentKit.Composite.CalendarViewMode>` | no | no | no |  |
| `WeekStart` | `Int32` | no | no | no | 0 = Sunday .. 6 = Saturday, matching fluent-svelte's weekStart. |

### `FluentCalendarViewItem`

Category: **Composite** · Sample: `/Composite/Calendar`
Ported from fluent-svelte's CalendarViewItem.svelte — a single round day/month/year cell button. Stateless: FluentCalendarView owns all selection/paging state and just tells each cell what to look like. @onmousedown:preventDefault is set unconditionally (same trick AutoSuggestBox's dropdown rows use) so clicking a cell inside CalendarDatePicker's self-contained popout doesn't blur the picker's trigger button before OnClick fires — that blur would otherwise closed the popout via focusout before the date selection registers.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `Blackout` | `Boolean` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `Current` | `Boolean` | no | no | no |  |
| `Disabled` | `Boolean` | no | no | no |  |
| `Header` | `String` | no | no | no | Small overline label shown above the cell content — the month abbreviation on the 1st day of a month (Days view) or the year on January (Months view), gated by FluentCalendarView's Headers parameter. |
| `InRange` | `Boolean` | no | no | no | Range-selection mode only: true when this cell lies strictly between the range's start and end (exclusive of both edges), rendered with a continuous band background instead of the round single/multiple selection pill. |
| `OnClick` | `Microsoft.AspNetCore.Components.EventCallback` | no | no | no |  |
| `OnKeyDown` | `EventCallback<Microsoft.AspNetCore.Components.Web.KeyboardEventArgs>` | no | no | no |  |
| `OutOfRange` | `Boolean` | no | no | no |  |
| `RangeEnd` | `Boolean` | no | no | no | Range-selection mode only: true when this cell is the range's end date. |
| `RangeStart` | `Boolean` | no | no | no | Range-selection mode only: true when this cell is the range's start date. |
| `Selected` | `Boolean` | no | no | no |  |
| `TabIndex` | `Int32` | no | no | no |  |
| `Variant` | `FluentKit.Composite.CalendarViewItemVariant` | no | no | no |  |

### `FluentCard`

Category: **Primitives** · Sample: `/Primitives/Card`
Generic surface primitive — not a literal WinUI control (WinUI doesn't ship a "Card"), but the CardBackgroundFillColorDefault / CardStrokeColorDefault tokens exist precisely because so many real controls (Settings cards, NavigationView content, Expander header/content) are built on this exact surface. Everything else that needs a bordered/tinted panel should compose this rather than repeat the background/border/radius trio inline.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |

### `FluentCheckBox`

Category: **Primitives** · Sample: `/Primitives/CheckBox`
Mirrors WinUI's CheckBox, including the three-state (indeterminate) mode used for "select all" style parent checkboxes. Built as a native ARIA checkbox rather than <input type="checkbox"> so the indeterminate visual state is fully CSS-driven (native inputs only expose indeterminate via JS interop on the DOM property, not an attribute).


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `Checked` | `Boolean` | no | no | yes |  |
| `CheckedChanged` | `EventCallback<Boolean>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `Disabled` | `Boolean` | no | no | no |  |
| `Indeterminate` | `Boolean` | no | no | yes | Third state, matching WinUI's IsThreeState CheckBox. When true, the glyph renders as a dash regardless of , and clicking cycles unchecked -> checked -> indeterminate. |
| `IndeterminateChanged` | `EventCallback<Boolean>` | no | no | no |  |
| `ThreeState` | `Boolean` | no | no | no |  |

### `FluentComboBox`

Category: **Composite** · Sample: `/Composite/ComboBox`
Mirrors fluent-svelte's ComboBox.svelte (dropdown list with an optional editable/searchable text-box mode). Two render modes off : - false (default): a FluentButton-shaped trigger showing the selected item's Name, opens a positioned <ul> of ComboBoxItem-equivalent <li> rows on click. - true: a FluentTextBox-shaped trigger the user can type into, filtering/matching items by Name prefix (case-insensitive), same as fluent-svelte's handleInput/searchValue flow. Unlike FluentMenuFlyout, this does NOT go through IOverlayService/FluentOverlayHost — the dropdown list needs to be exactly the trigger's width and grow from a specific list index (the menuOffset/menuGrowDirection choreography below), which is easiest to keep as a plain absolutely-positioned child of the component's own root (same as ComboBox.scss's own `position: relative` + `.combo-box-dropdown { position: absolute }`). A portal would only add indirection for zero benefit here since there's no anchor-to-elsewhere requirement.

Generic parameters: `TValue`

| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `Disabled` | `Boolean` | no | no | no |  |
| `Editable` | `Boolean` | no | no | no | Whether the ComboBox can be searched/typed into (fluent-svelte's `editable` prop). |
| `ItemTemplate` | `RenderFragment<ComboBoxItem<TValue>>` | no | no | no | Optional custom row content for each dropdown item. Falls back to a plain <span>@item.Name</span> when not supplied — same "text unless you say otherwise" default as fluent-svelte's own ComboBoxItem (which likewise only exposes a default slot, no separate templating concept). Wraps in the same .fluent-combo-box-item <li> (selection bar, hover/active states, focus ring) regardless of what you render inside — only the row's inner content is replaced, not its interaction chrome. |
| `Items` | `IReadOnlyList<ComboBoxItem<TValue>>` | no | no | no |  |
| `Placeholder` | `String` | no | no | no |  |
| `SearchValue` | `String` | no | no | yes | Current text in the search box. Only meaningful when is true. |
| `SearchValueChanged` | `EventCallback<String>` | no | no | no |  |
| `SelectedTemplate` | `RenderFragment<ComboBoxItem<TValue>>` | no | no | no | Optional custom content for the closed/collapsed trigger label in non-editable ( = false) mode, e.g. to show an icon + name instead of plain text once something is selected. Only used when there IS a selection — the placeholder text always renders plain regardless. Has no effect in Editable mode, since that trigger is a real <input> and can only ever hold plain text. |
| `Value` | `TValue` | no | no | yes | Currently selected item's value. Two-way bindable. |
| `ValueChanged` | `EventCallback<TValue>` | no | no | no |  |

### `FluentContentDialog`

Category: **Composite** · Sample: `/Composite/ContentDialog`
Mirrors fluent-svelte's ContentDialog.svelte (WinUI's ContentDialog) — a modal dialog centered over a full-viewport "smoke" scrim, with an optional Title, a scrollable body (ChildContent), and an optional Footer (typically 1-2 FluentButton instances laid out in an equal-width row, same as ContentDialog.scss's `.content-dialog-footer { grid-auto-flow: column }`). Deliberately NOT built on IOverlayService/FluentOverlayHost like FluentFlyout/FluentMenuFlyout/ FluentTooltip — those all position relative to a trigger ElementReference, whereas a modal dialog is always viewport-centered regardless of what invoked it, and needs its own full-screen scrim underneath (which the anchor-relative overlay model has no concept of). Self-contained, same reasoning as FluentComboBox's dropdown.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `Closable` | `Boolean` | no | no | no | Whether the dialog can be dismissed via the Escape key or by clicking the scrim. Set false to force the user through an explicit footer action (e.g. a destructive confirm dialog with no implicit "cancel by clicking away"). |
| `Darken` | `Boolean` | no | no | no | Whether the scrim behind the dialog darkens the rest of the page (SmokeFillColorDefault). Svelte's `darken` prop — false gives a fully transparent backdrop, still blocking interaction, just not visually dimmed. |
| `Footer` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Optional footer row, typically 1-2 FluentButton instances. Omit entirely for a dialog with no footer (matches svelte's `$$slots.footer` conditional render). |
| `IsOpen` | `Boolean` | no | no | yes | Two-way bindable open state. |
| `IsOpenChanged` | `EventCallback<Boolean>` | no | no | no |  |
| `OnBackdropClick` | `Microsoft.AspNetCore.Components.EventCallback` | no | no | no |  |
| `Size` | `FluentKit.Composite.ContentDialogSize` | no | no | no |  |
| `Title` | `String` | no | no | no | Title text shown as the dialog header (TextBlock Subtitle variant). Omit for a dialog with no header — just the body content. |

### `FluentContextMenu`

Category: **Composite** · Sample: `/Composite/ContextMenu`
Right-click context menu attached to arbitrary wrapped content (ContextMenu.svelte). Reuses FluentMenuFlyout's whole shape (IOverlayService + MenuFlyoutCloseContext + FluentMenuFlyoutSurface) rather than re-solving positioning: instead of porting fluent-svelte's own bespoke mousePosition/menuPosition clamp math, this points OverlayService.Show at a 0×0 fixed-position "cursor anchor" div moved to the click coordinates — the existing overlay-interop.js computePosition() (bottom-of-anchor, flips to top / clamps horizontally when there's no room) then does the exact same collision-avoidance job for free, with no new JS.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | The content that reveals the context menu on right-click. |
| `CloseOnSelect` | `Boolean` | no | no | no |  |
| `IsOpen` | `Boolean` | no | no | yes |  |
| `IsOpenChanged` | `EventCallback<Boolean>` | no | no | no |  |
| `MenuItems` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | The menu's items — FluentMenuFlyoutItem/FluentMenuFlyoutDivider elements. |

### `FluentDivider`

Category: **Primitives** · Sample: `/Design/SpacingAndGeometry`
Mirrors WinUI's plain content-area Divider (a hairline rule), not MenuFlyoutDivider — that one lives in the Composite/Overlay layer since it only makes sense inside a MenuFlyout's own padding rules. This is the general-purpose one for separating sections of ordinary page content.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `Vertical` | `Boolean` | no | no | no |  |

### `FluentDropDownButton`

Category: **Composite** · Sample: `/Composite/DropDownButton`
Mirrors WinUI's DropDownButton — a button that always opens a MenuFlyout rather than firing a click action itself (unlike SplitButton, which splits those two concerns into separate regions). Thin composition over with a button-styled, chevron-suffixed trigger in place of MenuFlyout's plain ChildContent span.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `Disabled` | `Boolean` | no | no | no |  |
| `IsOpen` | `Boolean` | no | no | yes |  |
| `IsOpenChanged` | `EventCallback<Boolean>` | no | no | no |  |
| `MenuItems` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | The menu's items — FluentMenuFlyoutItem/FluentMenuFlyoutDivider elements. |
| `Placement` | `FluentKit.Overlay.OverlayPlacement` | no | no | no |  |
| `Variant` | `FluentKit.Primitives.ButtonVariant` | no | no | no |  |

### `FluentExpander`

Category: **Primitives** · Sample: `/Primitives/Expander`
Expander — header + collapsible content area, header click/Enter/Space toggles. Animation uses fluent-svelte's clip+slide technique (max-block-size snap + translateY on the content) rather than the CSS-grid 0fr/1fr trick, which has inconsistent auto-minimum-size behavior across browsers (notably Safari) and can leave the row stuck above 0 with content still visible.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Header title content. |
| `Content` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Collapsible body content. |
| `Icon` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Optional leading icon in the header. |
| `IsExpanded` | `Boolean` | no | no | yes |  |
| `IsExpandedChanged` | `EventCallback<Boolean>` | no | no | no |  |

### `FluentFlyout`

Category: **Composite** · Sample: `/Composite/Flyout`
Generic positioned content flyout (WinUI 3 Flyout). Click/tap the trigger to open, click outside or press Escape to close. Built entirely on IOverlayService/FluentOverlayHost (Phase 3 infra) — this is the second overlay composite after FluentTooltip and the base every other overlay composite (MenuFlyout, ComboBox, TeachingTip, DropDownButton...) is expected to follow the same shape of.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | The element that opens the flyout when clicked. |
| `FlyoutContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | The content shown inside the flyout surface once open. |
| `IsOpen` | `Boolean` | no | no | yes | Two-way bindable open state, so callers can drive the flyout externally (e.g. open it from code, or close it from a "Save"/"Cancel" button inside FlyoutContent). |
| `IsOpenChanged` | `EventCallback<Boolean>` | no | no | no |  |
| `LightDismiss` | `Boolean` | no | no | no | Closes when clicking outside the flyout. Set false for flyouts that should only be dismissed programmatically or via an explicit action inside the content. |
| `Placement` | `FluentKit.Overlay.OverlayPlacement` | no | no | no | Preferred side of the trigger the flyout opens on; flips automatically if there's no room. |

### `FluentIcon`

Category: **Primitives** · Sample: `/Design/IconBrowser`
Renders a glyph from the MIT-licensed Fluent System Icons webfont (https://github.com/microsoft/fluentui-system-icons, © Microsoft Corporation) — see wwwroot/Icons/FluentSystemIcons-Regular.css. Replaces the earlier approach of wrapping SVG Icon instances from Microsoft.FluentUI.AspNetCore.Components.Icons: that package pulls in the full Microsoft.FluentUI.AspNetCore.Components core RCL as a transitive dependency, which auto-bundles its own scoped CSS into the host app's .styles.css and visually clashes with our tokens (see git history for the multi-hour static-web-assets fight that caused). The font approach has zero managed dependency and can't leak component CSS since it's just a CSS class. Name is the glyph identity as published in the font's own CSS, minus the "icon-" prefix — e.g. "ic_fluent_home_24_regular", "ic_fluent_settings_20_regular". Use for compile-time-checked constants covering common glyphs, or copy any class name straight out of wwwroot/Icons/FluentSystemIcons-Regular.css / the font's own icon gallery (fluentui-system-icons repo) for anything not listed there. The glyph's own baked-in size suffix (_16/_20/_24/...) only affects which pixel grid it was drawn on — below independently controls the actual rendered box, any combination works, but picking the closest baked-in size gives the crispest result. Decorative by default (aria-hidden) since icons are almost always paired with visible text (see NavigationViewItem); pass aria-hidden="false" plus your own aria-label via AdditionalAttributes for icon-only usage. Applications must link wwwroot/Icons/FluentSystemIcons-Regular.css once in their host page (e.g. index.html / App.razor), same as tokens.css: <link rel="stylesheet" href="_content/FluentKit/Icons/FluentSystemIcons-Regular.css" />


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `Name` | `String` | no | no | no |  |
| `Size` | `Int32` | no | no | no | Pixel size for both width and height of the icon box, and its font-size (the glyph scales with the font box). Independent of whatever size is baked into . |

### `FluentIconButton`

Category: **Primitives** · Sample: `/Primitives/IconButton`
Ported from fluent-svelte's IconButton.svelte — bare icon-only button, fixed square box (icon centered, no label padding). Renders as an anchor when is set (matching svelte's svelte:element this={href ? "a" : "button"} element-swap), otherwise a button. Supports the same set as (Standard/Accent/Subtle/Hyperlink) so icon-only and labeled buttons stay visually consistent — defaults to Subtle to preserve the original transparent-by-default look.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `Disabled` | `Boolean` | no | no | no |  |
| `Height` | `Nullable<Double>` | no | no | no |  |
| `Href` | `String` | no | no | no | When set (and not Disabled), renders an <a> instead of a <button>. |
| `MaxHeight` | `Nullable<Double>` | no | no | no |  |
| `MaxWidth` | `Nullable<Double>` | no | no | no |  |
| `MinHeight` | `Nullable<Double>` | no | no | no |  |
| `MinWidth` | `Nullable<Double>` | no | no | no |  |
| `OnClick` | `EventCallback<Microsoft.AspNetCore.Components.Web.MouseEventArgs>` | no | no | no |  |
| `Padding` | `Nullable<FluentKit.Common.Thickness>` | no | no | no | Inner spacing between the border and content, all sides. Unset = the control's CSS default. |
| `Variant` | `FluentKit.Primitives.ButtonVariant` | no | no | no |  |
| `Width` | `Nullable<Double>` | no | no | no |  |

### `FluentInfoBadge`

Category: **Primitives** · Sample: `/Primitives/InfoBadge`
Small non-intrusive notification/status dot or count pill. Empty (no ChildContent) renders as a plain 8px dot — used to flag "something changed" without a number; with ChildContent (typically a short number/text) it grows into a pill, same behavior as WinUI's InfoBadge Value/IconSource split.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `Severity` | `FluentKit.Primitives.InfoBadgeSeverity` | no | no | no |  |

### `FluentInfoBar`

Category: **Primitives** · Sample: `/Primitives/InfoBar`
App-wide status message banner — Title/Message text, an optional action, and (by default) a close button. Ported from fluent-svelte's InfoBar: reuses (same five-value severity ramp as FluentInfoBadge, which is also this component's default icon slot — same simplification fluent-svelte makes rather than pulling in WinUI's actual per-severity symbol glyphs) and the same --system-fill-color-*-background tokens the InfoBadge tokens comment already calls out as shared between the two components. Skips fluent-svelte's JS-measured wrap detection (offsetTop comparisons to add message-wrapped/action-wrapped classes when the title/message/action overflow onto their own line) — plain flexbox wrap gives acceptably close layout without a measurement pass, and this is a static banner, not something worth a resize-observer for.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `Action` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Optional call-to-action content (typically a FluentButton), right-aligned in the content row. |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Additional message content appended after — for rich content beyond plain text. |
| `Closable` | `Boolean` | no | no | no | Whether the built-in close button is rendered. |
| `Icon` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Overrides the default severity-colored icon. |
| `IsOpen` | `Boolean` | no | no | yes |  |
| `IsOpenChanged` | `EventCallback<Boolean>` | no | no | no |  |
| `Message` | `String` | no | no | no |  |
| `Severity` | `FluentKit.Primitives.InfoBadgeSeverity` | no | no | no |  |
| `Title` | `String` | no | no | no |  |

### `FluentListView`

Category: **Primitives** · Sample: `/Primitives/ListView`
Mirrors WinUI's ListView as a vertical item container. Owns roving-focus arrow-key navigation (Up/Down move focus between items, Home/End jump to the ends) via IFocusableListItem, same as before. Two composition modes, chosen by whether is set: - Data-bound (Items set): rows are generated from , with selection driven by and the SelectedValue/SelectedValues parameters below — same "Items + optional ItemTemplate" shape as FluentComboBox/FluentAutoSuggestBox. - Freeform (Items left null): the original ChildContent-only mode, where the caller places FluentListViewItem children manually and owns Selected/OnSelect itself (e.g. when selection needs to be driven by external context, the way FluentNavigationView's pane does it). TValue only matters for the data-bound mode — freeform callers can use any TValue (e.g. "object") since it's never referenced.

Generic parameters: `TValue`

| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `Class` | `String` | no | no | no | Extra class(es) appended to the root element. A plain parameter (not attribute splatting) so it composes instead of clobbering the base "fluent-list-view" class. |
| `ItemTemplate` | `RenderFragment<ListViewItem<TValue>>` | no | no | no | Optional custom row content for each item. Falls back to a plain @item.Name when not supplied, same default ComboBox/AutoSuggestBox use. Only consulted when is set. |
| `Items` | `IReadOnlyList<ListViewItem<TValue>>` | no | no | no | Data source for the data-bound mode. When set, rows are generated from this list instead of — see the type's own doc comment for the two modes. |
| `Role` | `String` | no | no | no | ARIA role for the container. "list" for a plain list, "listbox" if items are meant to read as a single-select widget to AT. |
| `SelectedValue` | `TValue` | no | no | yes | Currently selected value in mode. Two-way bindable. |
| `SelectedValueChanged` | `EventCallback<TValue>` | no | no | no |  |
| `SelectedValues` | `IReadOnlyList<TValue>` | no | no | yes | Currently selected values in mode. Two-way bindable. |
| `SelectedValuesChanged` | `EventCallback<IReadOnlyList<TValue>>` | no | no | no |  |
| `SelectionMode` | `FluentKit.Primitives.ListViewSelectionMode` | no | no | no | How many rows can be selected at once. Only meaningful in data-bound mode — freeform mode's selection is entirely caller-driven via each FluentListViewItem's own Selected/OnSelect. |

### `FluentListViewItem`

Category: **Primitives** · Sample: `/Primitives/ListView`
Single row inside a FluentListView. Selection is intentionally NOT owned here (see FluentListView's doc comment) — Selected is a plain bool the caller drives, OnSelect fires on click/Enter/Space (native <button> gives Space/Enter for free, same reasoning as FluentNavigationViewItem). Root element registers with the cascaded FluentListView purely for arrow-key roving focus (via IFocusableListItem); it works fine with no FluentListView ancestor too — List is optional.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `AutoHeight` | `Boolean` | no | no | no | Switches the row from ListView's default fixed 34px height to auto-height with a min-height floor — set by FluentListView<TValue> whenever a custom ItemTemplate is in play, since template content (e.g. a two-line contact row) is rarely exactly one line tall. |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `Disabled` | `Boolean` | no | no | no |  |
| `OnSelect` | `Microsoft.AspNetCore.Components.EventCallback` | no | no | no |  |
| `Selected` | `Boolean` | no | no | no |  |
| `ShowCheckmark` | `Boolean` | no | no | no | Shows a decorative checkbox glyph at the start of the row, reflecting . Set by FluentListView<TValue> when SelectionMode is Multiple — purely visual, the row's own button still owns the click (no nested interactive element), same reasoning FluentComboBoxItem's checkmark uses. |

### `FluentMenuBar`

Category: **Composite** · Sample: `/Composite/MenuBar`


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |

### `FluentMenuBarItem`

Category: **Composite** · Sample: `/Composite/MenuBar`
A single top-level entry in a FluentMenuBar (MenuBarItem.svelte) — e.g. "File"/"Edit"/"View" in an app menu bar. Opens its (typically FluentMenuFlyoutItem/FluentMenuFlyoutDivider children, reusing FluentMenuFlyoutSurface + MenuFlyoutCloseContext exactly like FluentMenuFlyoutItem's own cascading submenu does) below itself on click, Enter, or Space. Once one bar item's menu is open, hovering a sibling switches straight to its menu instead of requiring another click (svelte's handleMouseEnter/$currentMenu check), and ArrowLeft/ArrowRight walk between items with wraparound (svelte's sideNavigation context).


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | The item's label. |
| `Disabled` | `Boolean` | no | no | no |  |
| `Flyout` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | The dropdown's contents — typically FluentMenuFlyoutItem/FluentMenuFlyoutDivider elements. Omit for a plain non-interactive bar entry. |

### `FluentMenuFlyout`

Category: **Composite** · Sample: `/Composite/MenuFlyout`
Command menu opened from a trigger element (MenuFlyoutWrapper.svelte). Built on the same IOverlayService/FluentOverlayHost infra as FluentFlyout — this is effectively FluentFlyout with a MenuFlyoutCloseContext threaded through so its FluentMenuFlyoutItem children (and their own cascading submenus) know how to collapse the whole tree on selection. Note: fluent-svelte's MenuFlyoutWrapper also exposes an `alignment` (start/center/end) prop alongside placement. That's not something OverlayPlacement/overlay-interop.js currently model (they only pick which side of the anchor and clamp to the viewport) — left out here rather than half-implemented; add it to OverlayPlacement/computePosition if an application needs it.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | The element that opens the menu when clicked. |
| `Closable` | `Boolean` | no | no | no | Whether the menu can be dismissed by conventional interaction at all. |
| `CloseOnSelect` | `Boolean` | no | no | no | Whether selecting a standard/radio/toggle item closes the menu. Only applies if is true. |
| `IsOpen` | `Boolean` | no | no | yes |  |
| `IsOpenChanged` | `EventCallback<Boolean>` | no | no | no |  |
| `MatchAnchorWidth` | `Boolean` | no | no | no | When true, the menu is resized to exactly match the trigger's width (e.g. DropDownButton) instead of sizing to its own content. |
| `MenuItems` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | The menu's items — FluentMenuFlyoutItem/FluentMenuFlyoutDivider elements. |
| `Placement` | `FluentKit.Overlay.OverlayPlacement` | no | no | no |  |

### `FluentMenuFlyoutDivider`

Category: **Composite** · Sample: `/Composite/MenuFlyout`


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |

### `FluentMenuFlyoutItem`

Category: **Composite** · Sample: `/Composite/MenuFlyout`
A single row inside a FluentMenuFlyout/FluentContextMenu (MenuFlyoutItem.svelte). Three input "shapes" share one component, same as the Svelte original: Standard (plain command), Radio/Toggle (adds a bullet/checkmark glyph and a bindable ), and Cascading (opens a nested submenu instead of closing the tree on click). Radio mutual-exclusion is left to the caller (bind each item's own and flip siblings off in the click handler) rather than an internal group-value context — a menu's radio items are typically few and this keeps the item itself a plain, independently-bindable control, matching how little state fluent-svelte's own `group` binding actually needs here.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `Cascading` | `Boolean` | no | no | no | Marks this item as having a cascading submenu, rendering in a nested overlay opened to the right (flips left if there's no room) instead of closing the menu tree when clicked. |
| `Checked` | `Boolean` | no | no | yes | Bindable checked state for Radio/Toggle variants. |
| `CheckedChanged` | `EventCallback<Boolean>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | The item's label. |
| `Disabled` | `Boolean` | no | no | no |  |
| `Flyout` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Content for the cascading submenu. Only used when is true — typically more <FluentMenuFlyoutItem>/<FluentMenuFlyoutDivider> elements. |
| `Hint` | `String` | no | no | no | Secondary trailing text — e.g. a keyboard accelerator hint ("Ctrl+C"). |
| `Icon` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Optional leading icon slot. |
| `Indented` | `Boolean` | no | no | no | Indents the label to line up with sibling items that have icons, even if this one doesn't render an . |
| `OnClick` | `Microsoft.AspNetCore.Components.EventCallback` | no | no | no | Raised when a non-cascading item is activated (click, Enter, or Space). |
| `Selected` | `Boolean` | no | no | no | Styles the item as the current selection (e.g. the active view in a menu of views), independent of Radio/Toggle Checked state. |
| `Variant` | `FluentKit.Composite.MenuFlyoutItemVariant` | no | no | no |  |

### `FluentMenuFlyoutSurface`

Category: **Composite** · Sample: `/Composite/MenuFlyout`


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `OnKeyDown` | `EventCallback<Microsoft.AspNetCore.Components.Web.KeyboardEventArgs>` | no | no | no |  |

### `FluentMicaPanel`

Category: **Effects** · Sample: `/Effects/Mica`


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `BackgroundImageUrl` | `String` | no | no | no | Stand-in for the desktop wallpaper WinUI would otherwise sample directly via the OS compositor. Any image URL works (relative to wwwroot, or absolute). When omitted, the panel falls back to WinUI's own documented solid-color fallback. |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `Variant` | `FluentKit.Effects.MicaVariant` | no | no | no |  |

### `FluentNavigationView`

Category: **Composite** · Sample: `/Composite/NavigationView`


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `AlwaysShowHeader` | `Boolean` | no | no | no |  |
| `BackRequested` | `Microsoft.AspNetCore.Components.EventCallback` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `HeaderContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `IsBackButtonEnabled` | `Boolean` | no | no | no |  |
| `IsBackButtonVisible` | `Boolean` | no | no | no |  |
| `IsPaneOpen` | `Boolean` | no | no | yes |  |
| `IsPaneOpenChanged` | `EventCallback<Boolean>` | no | no | no |  |
| `ItemInvoked` | `EventCallback<Object>` | no | no | no |  |
| `MenuItems` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `PaneDisplayMode` | `FluentKit.Composite.NavigationViewPaneDisplayMode` | no | no | no |  |
| `PaneFooter` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `PaneHeader` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `PaneHeaderIcon` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `SelectedValue` | `Object` | no | no | yes |  |
| `SelectedValueChanged` | `EventCallback<Object>` | no | no | no |  |
| `SettingsRequested` | `Microsoft.AspNetCore.Components.EventCallback` | no | no | no |  |
| `ShowSettingsButton` | `Boolean` | no | no | no |  |

### `FluentNavigationViewItem`

Category: **Composite** · Sample: `/Composite/NavigationView`


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `Disabled` | `Boolean` | no | no | no |  |
| `Icon` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `IsExpanded` | `Boolean` | no | no | yes |  |
| `IsExpandedChanged` | `EventCallback<Boolean>` | no | no | no |  |
| `IsSelectable` | `Nullable<Boolean>` | no | no | no |  |
| `Text` | `String` | no | no | no |  |
| `Value` | `Object` | no | no | no |  |

### `FluentNavigationViewItemHeader`

Category: **Composite** · Sample: `/Composite/NavigationView`


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `Text` | `String` | no | no | no |  |

### `FluentNavigationViewItemSeparator`

Category: **Composite** · Sample: `/Composite/NavigationView`


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |

### `FluentNumberBox`

Category: **Composite** · Sample: `/Composite/NumberBox`
Mirrors WinUI's NumberBox / fluent-svelte's NumberBox.svelte — a validated numeric TextBox with spin up/down buttons wired through FluentTextBox's Buttons slot (the plumbing added for this). Composes FluentTextBox rather than reimplementing input chrome, same as fluent-svelte's own NumberBox does with its TextBox + TextBoxButton "buttons" slot. Text is only committed back to when it parses as a valid double — an in-progress edit like "-" or "3." is kept on screen as-is without round-tripping through Value, same "don't fight the user mid-keystroke" rule ComboBox's editable mode already established.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `Disabled` | `Boolean` | no | no | no |  |
| `Header` | `String` | no | no | no |  |
| `Max` | `Double` | no | no | no |  |
| `Min` | `Double` | no | no | no |  |
| `Mode` | `FluentKit.Composite.NumberBoxSpinButtonMode` | no | no | no | Compact: two small +/- buttons side by side, always inside the box. Expanded (default): no buttons at rest — a bigger stacked up/down control pops out from the box's edge on focus. |
| `Placeholder` | `String` | no | no | no |  |
| `Step` | `Double` | no | no | no |  |
| `Value` | `Double` | no | no | yes |  |
| `ValueChanged` | `EventCallback<Double>` | no | no | no |  |

### `FluentOverlayHost`

Category: **Infrastructure** · Sample: `/Composite/Flyout`


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |

### `FluentPasswordBox`

Category: **Primitives** · Sample: `/Primitives/PasswordBox`
Mirrors WinUI's PasswordBox, specifically its reveal-button variant: a right-aligned eye icon button that temporarily shows the plaintext value while held/toggled, rather than a bare <input type="password"> with no way to check what you typed. Composes + the same way NumberBox composes TextBox + TextBoxButton — swapping the underlying input's Type between "password" and "text" rather than reimplementing input chrome from scratch.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `Disabled` | `Boolean` | no | no | no |  |
| `Header` | `String` | no | no | no |  |
| `Placeholder` | `String` | no | no | no |  |
| `Value` | `String` | no | no | yes |  |
| `ValueChanged` | `EventCallback<String>` | no | no | no |  |

### `FluentPersonPicture`

Category: **Primitives** · Sample: `/Primitives/PersonPicture`
Ported from fluent-svelte's PersonPicture.svelte — round avatar. Shows <img Src> when set and loads successfully; falls back to initials derived from (or ChildContent, or a Badge slot) otherwise. Mirrors svelte's own error local-state-on-img-error fallback: if the image 404s/fails to decode, this flips to the initials div instead of showing a broken image icon, same as the source's on:error={() => (error = true)}.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `Alt` | `String` | no | no | no | Alt text for the image, and the source for the initials fallback (first letter of each space-separated word, uppercased) when there's no image or no . |
| `Badge` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Optional badge slot rendered in the top-right corner (e.g. a presence dot or notification count), matching fluent-svelte's slot="badge". |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Overrides the initials fallback with custom content (e.g. a glyph). |
| `Size` | `Int32` | no | no | no | Diameter in pixels of the round picture. |
| `Src` | `String` | no | no | no |  |

### `FluentPivot`

Category: **Composite** · Sample: `/Composite/Pivot`
Mirrors WinUI's Pivot/TabView (a horizontal strip of headers, one visible content pane at a time, arrow-key roving navigation between headers). Named FluentPivot rather than FluentTabView since that's the more fundamental WinUI 3 control the two largely share behavior with — TabView additionally supports closeable/add-new tabs, which can be layered on top of this later without changing the core header-strip/content-pane shape. The selection indicator (the blue underline) is a single absolutely-positioned div, not a border on each tab — sliding it between tabs on selection change needs its actual pixel position, which CSS alone can't derive from variable-width tab content, so wwwroot/Composite/Pivot/pivot-interop.js measures the selected button's offset/width and this re-renders with that baked into an inline transform/width (CSS transition handles the actual slide animation once those numbers land).


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `SelectedIndex` | `Int32` | no | no | yes |  |
| `SelectedIndexChanged` | `EventCallback<Int32>` | no | no | no |  |

### `FluentPivotItem`

Category: **Composite** · Sample: `/Composite/Pivot`
A single tab within a FluentPivot. See FluentPivot.razor.cs for the overall shape.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | The pane content shown while this tab is selected. |
| `Disabled` | `Boolean` | no | no | no |  |
| `TabTemplate` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Custom tab header content (e.g. an icon + text), overriding . |
| `Title` | `String` | no | no | no | Plain-text tab label — the common case. Ignored if is set. |

### `FluentProgressBar`

Category: **Primitives** · Sample: `/Primitives/ProgressBar`
Linear ProgressBar (ProgressBar_themeresources.xaml). Determinate mode fills to (0-100); with it plays WinUI's two-segment sliding-bar loop instead (pure CSS keyframe animation — no JS needed, unlike ProgressRing's arc math).


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `IsIndeterminate` | `Boolean` | no | no | no |  |
| `Value` | `Double` | no | no | no | Progress value 0-100. Ignored when is true. |

### `FluentProgressRing`

Category: **Primitives** · Sample: `/Primitives/ProgressRing`
Circular ProgressRing. Rewritten to match fluent-svelte's precise SVG circle-stroke-dashoffset math and CSS keyframe animations. Uses vector strokes rather than conic-gradient masks for maximum rendering sharpness and accurate animation easing curves.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `IsIndeterminate` | `Boolean` | no | no | no |  |
| `Size` | `Int32` | no | no | no | Diameter in pixels. |
| `Value` | `Double` | no | no | no | Progress value 0-100. Ignored when is true. |

### `FluentRadioButton`

Category: **Primitives** · Sample: `/Primitives/RadioButton`
Mirrors WinUI's RadioButton. Works two ways: 1. Standalone: bind directly (single independent radio, rare in practice). 2. Grouped (the common case): place inside a <FluentRadioGroup> and set — selection state then comes from the cascaded group context, matching fluent-svelte's bind:group pattern without relying on Blazor's InputRadioGroup (which requires EditContext).


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `Checked` | `Boolean` | no | no | yes |  |
| `CheckedChanged` | `EventCallback<Boolean>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `Disabled` | `Boolean` | no | no | no |  |
| `ItemTemplate` | `RenderFragment<Boolean>` | no | no | no | Full custom rendering for this option, replacing the default circle-and-dot indicator and label entirely. Receives whether this option is currently selected, so the template can style itself accordingly — e.g. a segmented "chip" row (a toolbar's Bold/Italic/Underline group, or a category picker) instead of a classic circle-and-label radio. The template still renders inside the same role="radio"/tabindex/keydown element as the default indicator, so whatever it renders stays keyboard-operable and announced correctly to a screen reader — you're only replacing the visual, not the selection semantics (still exactly one selected, never zero, exactly like the default indicator). |
| `Value` | `Object` | no | no | no |  |

### `FluentRadioGroup`

Category: **Primitives** · Sample: `/Primitives/RadioButton`
Groups a set of FluentRadioButton children so only one can be selected at a time — the container-level equivalent of WinUI's RadioButtons list control, minus the automatic layout (wrap child FluentRadioButtons in your own StackPanel-equivalent div for spacing/orientation).


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `Value` | `Object` | no | no | yes |  |
| `ValueChanged` | `EventCallback<Object>` | no | no | no |  |

### `FluentRevealBackground`

Category: **Effects** · Sample: `/Effects/Reveal`
Approximates WinUI's Reveal highlight — a soft radial-gradient spotlight that tracks the pointer across a control's border/background, used e.g. on NavigationView items and ListView rows to give hover feedback a sense of "light" rather than a flat color swap. Same effects layer as Mica/Acrylic, but unlike those two this one's actual visual work is a single CSS radial-gradient positioned via two custom properties (--reveal-x/--reveal-y) — the JS module (wwwroot/Effects/Reveal/reveal-interop.js) only measures pointer position, it does no rendering itself. additionally reveals along a 1px border ring (WinUI's "RevealBorderBrush" behavior), for use around a control's edge rather than its fill.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `Bordered` | `Boolean` | no | no | no | Also renders the highlight along a thin border ring instead of just the fill. |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |

### `FluentSettingsCard`

Category: **Composite** · Sample: `/Composite/SettingsCard`
1:1 port of the Windows Community Toolkit's SettingsCard (CommunityToolkit.WinUI.Controls.SettingsCard) — the base building block for consistent settings UI: an optional leading icon, a header/description stack, an end-aligned content slot (a ToggleSwitch, ComboBox, Button, etc.), and an optional trailing action icon (a chevron by default) that appears once the whole card is made clickable via . A FluentSettingsCard can also be hosted as an item inside a FluentSettingsExpander. WinUI's version reacts to available width with three extra VisualStates (RightWrapped, RightWrappedNoIcon, Vertical) driven by a ControlSizeTrigger. Blazor has no direct equivalent of that trigger, so the same three breakpoints (476px / 286px) are reproduced with CSS container queries on the card's own inline size in FluentSettingsCard.razor.css instead of C#-side layout code.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `ActionIcon` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | The icon shown at the trailing edge when and are both true. Defaults to a chevron, same as WCT. |
| `ActionIconToolTip` | `String` | no | no | no | Tooltip for . |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | The end-aligned content of the card (maps to WCT's Content property — a ToggleSwitch, Button, ComboBox, etc.). Named ChildContent so it can be passed as the component's ordinary child markup. |
| `ContentAlignment` | `FluentKit.Composite.SettingsCardContentAlignment` | no | no | no | The alignment of ChildContent. Defaults to , which keeps content end-aligned on one line and drops it below the header/description once the card gets too narrow for both to fit side by side. |
| `Description` | `String` | no | no | no | Description text (shown under the header, secondary/caption styling). Ignored when is supplied. |
| `DescriptionContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Description content, for anything beyond plain text (e.g. a HyperlinkButton). |
| `Disabled` | `Boolean` | no | no | no | Disables the card — dims foreground/icons, same as WinUI's Disabled visual state. |
| `Header` | `String` | no | no | no | Header text. Ignored when is supplied. |
| `HeaderContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Header content, for anything beyond plain text. |
| `HeaderIcon` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | The icon shown at the leading edge of the card. |
| `IsActionIconVisible` | `Boolean` | no | no | no | Whether the ActionIcon is shown (only relevant while is true). |
| `IsClickEnabled` | `Boolean` | no | no | no | Makes the whole card behave like a button — pointer/press visual states, keyboard activation (Enter/Space), and the trailing chevron/action icon become visible. |
| `OnClick` | `EventCallback<Microsoft.AspNetCore.Components.Web.MouseEventArgs>` | no | no | no | Fires when the card is activated (click, or Enter/Space while focused) and is true. |

### `FluentSettingsExpander`

Category: **Composite** · Sample: `/Composite/SettingsExpander`
1:1 port of the Windows Community Toolkit's SettingsExpander (CommunityToolkit.WinUI.Controls.SettingsExpander) — a collapsible header (itself a -shaped surface) that reveals a list of nested items underneath when expanded. WCT's version hosts its items via an ItemsRepeater bound to an Items/ItemsSource pair. Blazor has no ItemsRepeater equivalent worth reproducing here — the idiomatic approach is simply letting the caller pass the items as ordinary child markup (, typically a handful of <FluentSettingsCard> elements), the same way every other FluentKit composite accepts its children. and are preserved as-is since they're just extra render slots above/below the item list. The expand/collapse animation reuses FluentExpander's clip+slide technique (see that component's remarks) rather than the CSS-grid 0fr/1fr trick.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | The end-aligned content of the header row (maps to WCT's Content — commonly a ToggleSwitch that controls the whole group). Named ChildContent so it can be passed as the component's ordinary child markup. |
| `Collapsed` | `Microsoft.AspNetCore.Components.EventCallback` | no | no | no | Fires when the SettingsExpander is closed. |
| `Description` | `String` | no | no | no | Description text shown under the header. Ignored when is supplied. |
| `DescriptionContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Description content, for anything beyond plain text. |
| `Expanded` | `Microsoft.AspNetCore.Components.EventCallback` | no | no | no | Fires when the SettingsExpander is opened. |
| `Header` | `String` | no | no | no | Header text. Ignored when is supplied. |
| `HeaderContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Header content, for anything beyond plain text. |
| `HeaderIcon` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | The icon shown at the leading edge of the header. |
| `IsExpanded` | `Boolean` | no | no | yes | Whether the items area is expanded. |
| `IsExpandedChanged` | `EventCallback<Boolean>` | no | no | no |  |
| `ItemsContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | The nested settings cards shown once expanded (maps to WCT's Items/ItemsSource — typically a handful of <FluentSettingsCard> elements). |
| `ItemsFooter` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Optional content shown below , inside the expanded area. |
| `ItemsHeader` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Optional content shown above , inside the expanded area. |

### `FluentSlider`

Category: **Primitives** · Sample: `/Primitives/Slider`
Slider (Slider_themeresources.xaml / fluent-svelte's Slider.svelte). Drag tracking is delegated to wwwroot/Primitives/Slider/slider-interop.js — pointer/touch position comes back as a plain 0-100 rail percentage so this component never has to know pixel geometry itself, only how to turn a percentage into a stepped and back. Fixed-size 20px thumb (see .razor.css) — offset math for keeping the thumb from overflowing the rail edges is `halfWidth * (1 - 2*pct/100)`, ported from fluent-svelte's own clamp formula, with the 20px width hardcoded here rather than measured at runtime since it's a fixed CSS value.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `Disabled` | `Boolean` | no | no | no |  |
| `Max` | `Double` | no | no | no |  |
| `Min` | `Double` | no | no | no |  |
| `Orientation` | `FluentKit.Primitives.FluentSliderOrientation` | no | no | no |  |
| `Reverse` | `Boolean` | no | no | no | Flips which end of the rail 0/Max render at, without changing Min/Max semantics. |
| `ShowTooltip` | `Boolean` | no | no | no | Shows the live value in a small callout above/beside the thumb while dragging, hovering, or focused. Matches fluent-svelte's default behavior. |
| `Step` | `Double` | no | no | no |  |
| `TickFrequency` | `Double` | no | no | no | Gap between ticks, in Value units. 0 (default) draws no ticks. |
| `TickPlacement` | `FluentKit.Primitives.FluentSliderTickPlacement` | no | no | no |  |
| `Value` | `Double` | no | no | yes |  |
| `ValueChanged` | `EventCallback<Double>` | no | no | no |  |

### `FluentSplitButton`

Category: **Composite** · Sample: `/Composite/SplitButton`
Mirrors WinUI's SplitButton — a primary action region (fires immediately, like a plain button) joined to a separate narrow chevron region that opens a MenuFlyout of related actions, rendered as one visually-continuous pill (shared border, no gap, only the chevron side has rounded outer corners on its own edge — see .razor.css). Unlike , which is nothing but a menu trigger, SplitButton's main region is a real independent action. FluentMenuFlyout's trigger wraps the WHOLE two-button pill (not just the chevron) so its anchor width is the full control's width — needed for MatchAnchorWidth to stretch the menu to the button's own width rather than the chevron's ~32px. The action button stops its click from bubbling to that wrapping trigger (@@onclick:stopPropagation) so clicking it fires only ; the chevron has no handler of its own and relies on the bubble to open the menu.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `Disabled` | `Boolean` | no | no | no |  |
| `IsOpen` | `Boolean` | no | no | yes |  |
| `IsOpenChanged` | `EventCallback<Boolean>` | no | no | no |  |
| `MenuItems` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | The chevron's menu items — FluentMenuFlyoutItem/FluentMenuFlyoutDivider elements. |
| `OnClick` | `EventCallback<Microsoft.AspNetCore.Components.Web.MouseEventArgs>` | no | no | no |  |
| `Placement` | `FluentKit.Overlay.OverlayPlacement` | no | no | no |  |
| `Variant` | `FluentKit.Primitives.ButtonVariant` | no | no | no |  |

### `FluentTeachingTip`

Category: **Composite** · Sample: `/Composite/TeachingTip`
Mirrors WinUI's TeachingTip — a persistent, anchor-relative callout (title/subtitle, an optional row of action buttons, and an explicit close button) used for contextual "here's what this does" UI, as opposed to Flyout's generic content or ToolTip's hover-only hint. Structurally close to Flyout: built directly on IOverlayService/FluentOverlayHost with the same programmatic--binding shape as , and — like every other flyout/menu/tooltip — relies entirely on OverlaySurface's own default chrome (background, backdrop-filter: blur(60px), border, shadow) for its blur; it does NOT pass bare. One real difference from Flyout: it adds its own pointer/beak toward the anchor in targeted mode (WinUI teaching tips always draw one there — see the beak's own matching background/border in FluentTeachingTip.razor.css), and it defaults to false — a teaching tip is meant to stay up until the person reads it and dismisses it via the explicit close button, not disappear the moment they click elsewhere. Two placement modes, chosen automatically by whether is supplied: Targeted ( set) — is the anchor/target element the tip points its beak at, positioned via (top/bottom/left/right of the anchor), same anchor-measurement path as Flyout/MenuFlyout. Unlike Flyout/MenuFlyout it is NOT wrapped in a click handler — teaching tips are shown/hidden programmatically (e.g. after a first-run check), never by clicking their own target.Detached ( omitted) — for a tip that is not pointing at any particular control (e.g. a general new-features announcement). No beak is drawn, and position is set by relative to the viewport (defaults to bottom-center) via instead of the anchor-based overload.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `ActionContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Optional row of action buttons (e.g. "Got it" / "Learn more") below the text. |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Content wrapped in the anchor span. Historically this also doubled as the anchor itself, which breaks the moment the thing you want the tip to point at isn't the same element you clicked to trigger it (e.g. a toolbar button that should show a tip pointing at a card somewhere else on the page). Prefer for that case — leave ChildContent unset when using Target, or set both if you also want the tip to wrap some markup for its own sake. |
| `IsOpen` | `Boolean` | no | no | yes |  |
| `IsOpenChanged` | `EventCallback<Boolean>` | no | no | no |  |
| `LightDismiss` | `Boolean` | no | no | no | Closes on outside click. Defaults false — teaching tips are dismissed explicitly. |
| `Placement` | `FluentKit.Overlay.OverlayPlacement` | no | no | no | Placement relative to the anchor. Only meaningful in targeted mode (i.e. when is set) — ignored in detached mode, where is used instead. |
| `ScreenPlacement` | `FluentKit.Overlay.OverlayScreenPlacement` | no | no | no | Placement relative to the viewport for the detached variant (i.e. when is NOT set). Ignored in targeted mode. Defaults to , matching WinUI's own default spot for an untargeted teaching tip. |
| `Subtitle` | `String` | no | no | no |  |
| `Target` | `Nullable<Microsoft.AspNetCore.Components.ElementReference>` | no | no | no | Explicit element the tip's beak points at, independent of whatever triggered . Takes priority over 's own wrapper span as the anchor when both are set. If the target isn't fully visible in the viewport when the tip opens, it's scrolled into view (centered, instant) first — there's no guarantee it's already on screen the way a Flyout's own trigger always is. If the target element is later removed from the DOM while the tip is still open, the tip auto-closes rather than floating in place pointing at nothing. |
| `Title` | `String` | no | no | no |  |

### `FluentTextBlock`

Category: **Primitives** · Sample: `/Primitives/TextBlock`


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `Class` | `String` | no | no | no |  |
| `Tag` | `String` | no | no | no | Overrides the default HTML tag for this variant. Supported: span, div, p, h1-h6. |
| `Variant` | `FluentKit.Primitives.TextBlockVariant` | no | no | no |  |

### `FluentTextBox`

Category: **Primitives** · Sample: `/Primitives/TextBox`
Mirrors WinUI's TextBox. The focus/underline behavior (an accent-colored bottom edge that only appears once focused) is pure CSS via :focus-within on the container in FluentTextBox.razor.css — see TextBox.scss in fluent-svelte for the reference implementation this was ported from (that one uses an ::after pseudo-element the same way).


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `Buttons` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no | Optional right-aligned button slot (mirrors fluent-svelte's TextBox slot="buttons"). Used e.g. by NumberBox for its spin up/down buttons, or by applications for a clear/search icon. |
| `Disabled` | `Boolean` | no | no | no |  |
| `Header` | `String` | no | no | no |  |
| `Placeholder` | `String` | no | no | no |  |
| `Type` | `String` | no | no | no |  |
| `Value` | `String` | no | no | yes |  |
| `ValueChanged` | `EventCallback<String>` | no | no | no |  |

### `FluentTextBoxButton`

Category: **Primitives** · Sample: `/Primitives/TextBox`
Small icon-only button meant to live inside FluentTextBox's slot — mirrors fluent-svelte's TextBoxButton.svelte. Reuses the subtle-fill ramp (same as FluentButton's Subtle variant) rather than the full bordered Button chrome, since it sits inside the textbox's own border. Also supports press-and-hold repeat (used by NumberBox's spin buttons): holding the pointer down re-invokes repeatedly after an initial delay, accelerating the interval on each repeat down to a floor, same "hold to speed up" behavior as WinUI's own NumberBox / OS spinner controls. A plain tap still only fires OnClick once (the immediate @onclick), since the repeat loop's first extra invocation only lands after .


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `Disabled` | `Boolean` | no | no | no |  |
| `InitialHoldDelayMs` | `Int32` | no | no | no | Delay before hold-repeat kicks in after pointerdown, in milliseconds. |
| `OnClick` | `EventCallback<Microsoft.AspNetCore.Components.Web.MouseEventArgs>` | no | no | no |  |
| `RepeatAccelerationFactor` | `Double` | no | no | no | Multiplier applied to the interval after every repeat (below 1 = speeding up). |
| `RepeatMinIntervalMs` | `Int32` | no | no | no | Floor the repeat interval accelerates down to, in milliseconds. |
| `RepeatStartIntervalMs` | `Int32` | no | no | no | Interval between repeats at the start of a hold, in milliseconds. |

### `FluentTimePicker`

Category: **Composite** · Sample: `/Composite/TimePicker`
A brand-new control, not a fluent-svelte port (fluent-svelte has no TimePicker) — modeled directly on WinUI 3's TimePicker + TimePickerFlyout (see WinUI-Gallery's TimePickerPage.xaml and microsoft-ui-xaml's control template) rather than any existing web time-picker convention. Key WinUI behaviors reproduced: - Three snap-scrolling columns (Hour, Minute, and AM/PM unless is TwentyFourHour) with the vertically-centered row being the "selected" one — WinUI's flyout uses a LoopingSelector per column; the web has no equivalent primitive, so this is built as a plain scroll-snap-align:center list per column with IntersectionObserver-free selection tracking done by comparing each item's scroll offset against the container's on scroll (see the JS interop module) — good enough fidelity without inventing a full virtualized looping selector for a first cut. - Values are staged locally while the flyout is open (WinUI: dragging the columns doesn't commit until you tap the accept/checkmark glyph) and only pushed to on Accept; Cancel (or Escape, or outside click) discards the in-progress scroll position and reverts to the last committed value. - matches WinUI's property of the same name — restricts the minute column to that step (e.g. 15 => :00/:15/:30/:45 only). Architecture matches this repo's other trigger+popout composites (FluentCalendarDatePicker, FluentComboBox): self-contained absolutely-positioned popout, not routed through IOverlayService.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `ClockIdentifier` | `FluentKit.Composite.TimeClockIdentifier` | no | no | no | 12-hour (default, with AM/PM column) or 24-hour. Matches WinUI's ClockIdentifier. |
| `Disabled` | `Boolean` | no | no | no |  |
| `Header` | `String` | no | no | no | Optional label shown above the trigger button, matching WinUI TimePicker's Header. |
| `Locale` | `String` | no | no | no |  |
| `MinuteIncrement` | `Int32` | no | no | no | Restricts the minute column to this step. Matches WinUI's MinuteIncrement (legal range there is 1-30; enforced the same way here via ). |
| `Placeholder` | `String` | no | no | no |  |
| `SelectedTime` | `Nullable<TimeSpan>` | no | no | yes | The committed time-of-day. Two-way bindable. Null means "no time picked yet" and shows on the trigger button, matching WinUI's own unset TimePicker. |
| `SelectedTimeChanged` | `EventCallback<Nullable<TimeSpan>>` | no | no | no |  |
| `TimeChanged` | `EventCallback<Nullable<TimeSpan>>` | no | no | no | Fires only when the user accepts a new value via the flyout's checkmark button — mirrors WinUI TimePicker's SelectedTimeChanged event semantics (as distinct from the two-way bind itself), matching FluentCalendarView's own ValueChanged/Change split convention. |

### `FluentToggleButton`

Category: **Primitives** · Sample: `/Primitives/ToggleButton`
Mirrors WinUI's ToggleButton — a button that stays visually "pressed" once activated, distinct from (which always pairs its state with On/Off text per Fluent's accessibility guidance) and from plain (momentary, no persisted state). Typical uses: formatting toolbars (Bold/Italic), a single standalone "Mute" button, etc.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `Checked` | `Boolean` | no | no | yes |  |
| `CheckedChanged` | `EventCallback<Boolean>` | no | no | no |  |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `Disabled` | `Boolean` | no | no | no |  |

### `FluentToggleSwitch`

Category: **Primitives** · Sample: `/Primitives/ToggleSwitch`
Mirrors WinUI's ToggleSwitch, including the optional Header/OnContent/OffContent text WinUI shows alongside the physical switch (unlike a plain CheckBox, ToggleSwitch always communicates its current state via text, not just position, per Fluent's accessibility guidance).


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `AdditionalAttributes` | `IReadOnlyDictionary<String, Object>` | no | no | no |  |
| `Checked` | `Boolean` | no | no | yes |  |
| `CheckedChanged` | `EventCallback<Boolean>` | no | no | no |  |
| `Disabled` | `Boolean` | no | no | no |  |
| `Header` | `String` | no | no | no |  |
| `OffContent` | `String` | no | no | no |  |
| `OnContent` | `String` | no | no | no |  |

### `FluentTooltip`

Category: **Composite** · Sample: `/Composite/Tooltip`
Deliberately the simplest possible use of IOverlayService/FluentOverlayHost — this exists to prove the overlay infrastructure end to end (Phase 3 exit criteria), not as the final Tooltip. Missing, on purpose, for now: show/hide delay debouncing, and re-showing on scroll.


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |
| `Text` | `String` | no | no | no |  |

### `OverlaySurface`

Category: **Infrastructure** · Sample: `/Composite/Flyout`


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `Entry` | `FluentKit.Overlay.OverlayEntry` | no | no | no |  |

### `ThemeProvider`

Category: **Infrastructure** · Sample: `/Design/Colors`


| Parameter | Type | Cascading | RenderFragment | Bindable | Description |
| --- | --- | --- | --- | --- | --- |
| `ChildContent` | `Microsoft.AspNetCore.Components.RenderFragment` | no | yes | no |  |

## Public types

| Type | Kind | Members | Description |
| --- | --- | --- | --- |
| `SizableComponentBase` | class | `Height`, `MaxHeight`, `MaxWidth`, `MinHeight`, `MinWidth`, `Padding`, `Width` | Base class for controls that expose XAML-style Padding/Width/Height/ MinWidth/MinHeight/MaxWidth/MaxHeight overrides (mirrors FrameworkElement's sizing properties in WPF/WinUI). Every property here is nullable and by default, meaning "unset — fall back to the control's own stylesheet default"; only the ones an application actually sets get written into . Inheritors render style="@SizingStyle" on their root element (in addition to, not instead of, their existing CSS classes) so overrides win via normal inline-style specificity without needing !important or duplicated CSS. |
| `Thickness` | struct | `Bottom`, `Equals`, `Equals`, `GetHashCode`, `Left`, `Right`, `ToCss`, `Top`, `Zero` | Mirrors XAML's Thickness (used for Padding/Margin in WPF/WinUI) — same three constructor shapes (uniform, horizontal/vertical, four-value), same Left/Top/Right/Bottom field names. Values are CSS pixels. Renders to a CSS padding shorthand via — note CSS shorthand order is top/right/bottom/left, not left/top/right/bottom, so the constructor argument order and the emitted order intentionally differ; that's a CSS convention mismatch inherited from the platform, not a bug here. |
| `AutoSuggestBoxItem`1` | class | `Deconstruct`, `Disabled`, `Equals`, `Equals`, `GetHashCode`, `Name`, `ToString`, `Value` | One suggestion row in a . Same shape as (Name/Value/Disabled) — kept as its own type rather than reusing ComboBoxItem directly since AutoSuggestBox's "Value" is conceptually a query result payload rather than a fixed option, even though the record looks identical today. |
| `CalendarSelectionMode` | enum | `Multiple`, `Range`, `Single` | Selection behavior for and . Not part of fluent-svelte's original API (it only ever exposed a multiple boolean) — added here as a superset so applications can opt into a third, common date-range picking pattern (à la Fluent UI React's DatePicker range selection) without overloading a bool. |
| `CalendarViewItemVariant` | enum | `Day`, `MonthYear` | Matches fluent-svelte's CalendarViewItem variant prop — day cells are round/small (40px), month/year cells are round/larger (56px), per CalendarViewItem.scss's two .type- sizes. |
| `CalendarViewMode` | enum | `Days`, `Months`, `Years` | The three drill levels fluent-svelte's CalendarView cycles through — clicking the header goes Days → Months → Years (and Years is a dead end, its header button is disabled); picking a month drops back to Days, picking a year drops back to Months. |
| `ComboBoxItem`1` | class | `Deconstruct`, `Disabled`, `Equals`, `Equals`, `GetHashCode`, `Name`, `ToString`, `Value` | One selectable entry in a . Mirrors fluent-svelte's ComboBox Item interface (name/value/disabled) — Name is the display text used both for the visible label and (in mode) the starts-with search match against . |
| `ContentDialogSize` | enum | `Max`, `Min`, `Standard` | Visual size of a , mirroring fluent-svelte's ContentDialog `size` prop (min/standard/max) and WinUI's own three dialog width presets. |
| `MenuBarContext` | class | `ClearOpen`, `CurrentOpenItem`, `FocusAdjacentAsync`, `Register`, `SetOpen`, `Unregister` | Shared cascading state for a FluentMenuBar tree — ported from fluent-svelte's currentMenu writable store (MenuBar/flyoutState.ts) plus MenuBar.svelte's sideNavigation context. Tracks which top-level item currently has its dropdown open (so hovering a sibling switches the open menu instead of requiring another click, matching svelte's $currentMenu check) and the left-to-right tab order of registered items (for ArrowLeft/ArrowRight wraparound focus, matching svelte's tabbable-based sideNavigation handler — done here via item self-registration instead of a live DOM query, since that's how this project's other roving-tabindex controls (CalendarView, NavigationView) already do it). |
| `MenuFlyoutCloseContext` | class | `Closable`, `CloseAll`, `CloseOnSelect`, `Subscribe` | Shared close-coordination for a MenuFlyout/ContextMenu tree. A single instance is created by the root (FluentMenuFlyout or FluentContextMenu) and cascaded down through every nested submenu level. Each open submenu subscribes its own close action to so that selecting a leaf item anywhere in the tree collapses the whole chain at once — mirroring fluent-svelte's single `closeFlyout` context, just fanned out to however many submenu levels are currently open (each one is a *separate* IOverlayService entry, so there's no automatic parent/ child unmounting to piggyback on the way there would be with plain nested DOM). |
| `MenuFlyoutIconGutterContext` | class | `HasAnyIcon`, `RegisterIcon`, `UnregisterIcon` | Per-menu-level (one instance per FluentMenuFlyoutSurface) tally of how many of its FluentMenuFlyoutItem children currently render an . Exists so a menu with NO icons anywhere doesn't reserve the leading icon gutter on every row — WinUI/fluent-svelte only indent labels to line up with icons when at least one sibling actually has one; a menu that's icon-free throughout should have its labels start flush left. This is inherently a two-pass problem: an item can't know at its own render time whether some *later* sibling has an icon, since siblings render in document order within the same pass. Items register/unregister as their own Icon presence changes; the count transitioning 0->1 (or back) raises , which FluentMenuFlyoutSurface subscribes to and reacts to with its own StateHasChanged() — forcing a second render pass in which every item now sees the final, correct value. A menu that never gets any icons only ever needs the one pass; a menu whose first-rendered item already has an icon effectively self-corrects on the second pass too, harmlessly. |
| `MenuFlyoutItemVariant` | enum | `Radio`, `Standard`, `Toggle` |  |
| `NavigationViewContext` | class | `Dispose`, `SelectValue`, `SelectedValue` |  |
| `NavigationViewDisplayMode` | enum | `Compact`, `Expanded`, `Minimal` |  |
| `NavigationViewPaneDisplayMode` | enum | `Auto`, `Left`, `LeftCompact`, `LeftMinimal`, `Top` |  |
| `NumberBoxSpinButtonMode` | enum | `Compact`, `Expanded` | Controls how NumberBox's spin buttons are presented — see FluentNumberBox.razor.cs remarks. |
| `PivotContext` | class | `Items`, `NotifyChanged`, `Register`, `Unregister` | Shared registration list for a FluentPivot tree. Pivot items are data-only — they render nothing themselves (see FluentPivotItem) and just register their Title/TabTemplate/ChildContent here; FluentPivot owns all actual DOM output (the tab strip buttons + the single active content pane), reading straight out of this list by index rather than each item rendering itself hidden/shown. Registration happens during the items' own OnInitialized, which runs one render pass after FluentPivot's own — triggers that follow-up render so the tab strip picks the items up as soon as they've registered, same pattern MenuBarContext uses for its list. |
| `SettingsCardContentAlignment` | enum | `Auto`, `Left`, `Right`, `Vertical` | The alignment of a FluentSettingsCard's ChildContent. |
| `TimeClockIdentifier` | enum | `TwelveHour`, `TwentyFourHour` | Mirrors WinUI TimePicker/TimePickerFlyout's ClockIdentifier property — which in WinUI is a loosely-typed string ("12HourClock" / "24HourClock") since it's really a Windows.Globalization clock system identifier. Re-exposed here as a proper enum (FluentKit convention — see , ) since a string parameter with exactly two legal values isn't worth the stringly-typed footgun for a net-new (non-ported) control that owes WinUI fidelity, not fluent-svelte source compatibility. |
| `AcrylicKind` | enum | `Base`, `Thin` | Approximates WinUI's in-app Acrylic (AcrylicBrush / DesktopAcrylicBackdrop). This is a genuinely different material from Mica (see FluentMicaPanel) — Acrylic is TRANSLUCENT and reacts live to whatever's actually rendered behind it (in-app content scrolling underneath, other panels, etc.), not a static sample of the desktop wallpaper. On the web the correct primitive for that is CSS `backdrop-filter`, which live-samples the page content behind the element and blurs it in real time. This component used to be (incorrectly) named FluentMicaPanel — that name now belongs to the actual wallpaper-based static-blur material, which needed a from-scratch implementation instead. |
| `MicaVariant` | enum | `Base`, `BaseAlt` | Approximates WinUI's Mica material — rebuilt from scratch against the actual recipe in microsoft-ui-xaml's SystemBackdropComponentInternal::BuildMicaEffectBrush (dev/dll/SystemBackdrop/SystemBackdropBrushFactory.cpp). This is NOT the same thing as Acrylic (see FluentAcrylicBrush) — Acrylic live-blurs whatever's actually behind it via backdrop-filter; Mica does not. PERFORMANCE NOTE: this used to run the blur (CSS `filter`) and the two blend passes (CSS `mix-blend-mode`) live, every repaint. That's expensive for a large/fixed-viewport element and was the source of noticeable jank. It's now baked ONCE per (image, variant, theme) into a static raster via an offscreen <canvas> (see wwwroot/Effects/mica-interop.js) and cached — after that, rendering Mica is just displaying a static image, no different in cost from any other photo background. The recipe itself (blur -> luminosity blend -> color blend) is unchanged, just baked ahead of time instead of recomputed on every paint. 1. Start from a heavily blurred copy of the desktop wallpaper (ICompositorWithBlurredWallpaper- BackdropBrush — an OS compositor API that samples the wallpaper only, not other windows). Browsers can't reach the real wallpaper, so stands in for it — supply any image (ideally the user's own wallpaper file) and it's blurred the same way. 2. LUMINOSITY blend pass: blend TintColor (alpha = LuminosityOpacity) over the blurred wallpaper using true "luminosity" blend mode — keeps the wallpaper's hue/saturation but replaces its per-pixel luminosity with the tint's. Mica's defaults set LuminosityOpacity to 1.0, so this pass ends up governing almost the entire final look. 3. COLOR blend pass: blend TintColor (alpha = TintOpacity) over the result of step 2 using true "color" blend mode — keeps the TINT's hue/saturation but takes luminosity from step 2's result. This is what gives Mica its "muted, personalized" gradient-ish look: the wallpaper's light/dark shape survives, but its color is replaced by the theme tint. With no BackgroundImageUrl supplied (or a format the browser can't decode, e.g. HEIC outside Safari), this degrades to WinUI's own documented fallback: a flat SolidBackgroundFillColorBase (Alt) fill — the same state real Mica falls back to under RDP, low-power GPUs, or Battery Saver. |
| `IOverlayService` | interface | `Active`, `Close`, `CloseAll`, `CompleteClose`, `RefreshContent`, `Show`, `ShowDetached`, `Update` | Register as Scoped. Components never position themselves — they call Show() with their own ElementReference as the anchor and let FluentOverlayHost (mounted once, near the app root) render the content at a fixed top-level position. This is the Blazor answer to React's createPortal: render-tree teleportation via a cascading service, not DOM manipulation. |
| `OverlayEntry` | class | `Anchor`, `Bare`, `ComputedStyle`, `Content`, `Id`, `IsClosing`, `IsDetached`, `LightDismiss`, `MatchAnchorWidth`, `PreferredPlacement`, `ScrollAnchorIntoView`, `WatchAnchorRemoved` |  |
| `OverlayPlacement` | enum | `Bottom`, `Left`, `Right`, `Top` | Preferred placement relative to the anchor element. The JS interop layer (overlay-interop.js) flips this to the opposite side when there isn't room in the viewport — callers get the flipped result back, they don't need to reason about collision themselves. |
| `OverlayScreenPlacement` | enum | `BottomCenter`, `BottomLeft`, `BottomRight`, `Center`, `TopCenter`, `TopLeft`, `TopRight` | Placement for a "detached" overlay that has no anchor element to position relative to (see ) — positioned relative to the viewport instead, via a plain CSS `position: fixed` computed synchronously in (unlike the anchor-based path, this needs no JS measurement round-trip, since there's no anchor rect to measure — the overlay can be positioned correctly on its very first render instead of one frame later). |
| `OverlayService` | class | `Active`, `Close`, `CloseAll`, `CompleteClose`, `RefreshContent`, `Show`, `ShowDetached`, `Update` |  |
| `ButtonVariant` | enum | `Accent`, `Hyperlink`, `Standard`, `Subtle` | Mirrors WinUI's Button / AccentButton (via the IsAccent-equivalent style) / HyperlinkButton split. "Subtle" matches what WinUI calls a SubtleButton-styled Button (no border, transparent by default). |
| `FluentIconNames` | class |  | Hand-picked, ergonomic aliases for the most commonly used Fluent System Icons glyphs (e.g. Name="@FluentIconNames.Home"). For the full, exhaustive set of ~9,600 glyphs (auto-generated from wwwroot/Icons/FluentSystemIcons-Regular.css by tools/generate-icon-names.py), see the other partial of this class in FluentIconNames.g.cs, e.g. FluentIconNames.Beaker20. |
| `FluentSliderOrientation` | enum | `Horizontal`, `Vertical` |  |
| `FluentSliderTickPlacement` | enum | `Inline`, `None`, `Outside` |  |
| `IFocusableListItem` | interface | `Disabled`, `FocusAsync` | Minimal contract FluentListView needs to drive roving arrow-key focus across its children — deliberately NOT tied to FluentListViewItem's concrete type, so other composites (e.g. FluentNavigationViewItem, which owns its own selection via NavigationViewContext instead of FluentListViewItem's plain Selected/OnSelect) can register into the same list for keyboard nav without adopting FluentListViewItem's markup or selection model. |
| `IListViewHost` | interface | `FocusAdjacentAsync`, `FocusEndAsync`, `Register`, `Unregister` | Non-generic contract FluentListViewItem cascades against. FluentListView<TValue> is a different closed generic type per TValue (FluentListView<string> and FluentListView<int> don't share a type), so a plain [CascadingParameter] FluentListView<TValue>? on FluentListViewItem could never bind to it — FluentListViewItem itself has no TValue to close over. Cascading this interface instead sidesteps that: any FluentListView<TValue> implements it the same way regardless of TValue. |
| `InfoBadgeSeverity` | enum | `Attention`, `Caution`, `Critical`, `Informational`, `Success` |  |
| `ListViewItem`1` | class | `Deconstruct`, `Disabled`, `Equals`, `Equals`, `GetHashCode`, `Name`, `ToString`, `Value` | One row of data in a data-bound — same Name/Value/Disabled shape as and , kept as its own type (rather than shared) since ListView lives in Primitives while those two are Composite, and each already treats its own record as the "one row of data" contract for that control. |
| `ListViewSelectionMode` | enum | `Multiple`, `None`, `Single` | Mirrors WinUI ListView's SelectionMode — how many rows can be selected at once, and what visual affordance each row gets. WinUI also has "Extended" (click = single, Ctrl/Shift-click = range/multi), which is a desktop mouse-modifier interaction model that doesn't map cleanly onto a plain click/tap row — left out here in favor of the two modes that do (Single/Multiple), same reasoning ComboBox uses for not exposing multi-select. |
| `RadioGroupContext` | class | `SelectedValue`, `SetValueAsync` | Cascaded context a FluentRadioGroup exposes to its child FluentRadioButtons, mirroring the "group" binding fluent-svelte's RadioButton exposes via bind:group — Blazor has no native equivalent to Svelte's cross-component two-way binding, so a small explicit context does the job. |
| `TextBlockVariant` | enum | `Body`, `BodyLarge`, `BodyStrong`, `Caption`, `Display`, `Subtitle`, `Title`, `TitleLarge` | Mirrors WinUI's TextBlock / fluent-svelte's TextBlock: a single component for every text style in the type ramp, each with a sensible default semantic tag (Subtitle -> h4, Body -> span, etc.) that can override — e.g. force a Title-styled span instead of an h3. |
| `AccentColorService` | class | `CurrentAccent`, `CurrentPalette`, `DisposeAsync`, `InitializeAsync`, `SetAccentAsync`, `SetAccentFromImageAsync` | See . Same DI lifetime story as (Scoped). |
| `AccentPalette` | class | `Base`, `Dark1`, `Dark2`, `Dark3`, `Fallback`, `FallbackBase`, `FromColor`, `FromHex`, `Light1`, `Light2`, `Light3`, `ToCssVariables` | The 7-step ramp WinUI derives from a single "system accent color": the base color plus three lightened steps (used as the accent ramp in dark theme, where fills/text need to get *brighter* than the base to read well on dark backgrounds) and three darkened steps (used in light theme, where fills/text need to get *darker* than the base to read on light backgrounds). NOTE ON FIDELITY: real WinUI (Microsoft.UI.Xaml.Media.AccentColors / the "Fluent XAML Theme Editor") computes this ramp with an internal, undocumented algorithm that isn't public source. This is a from-scratch approximation — simple linear tint/shade mixing in sRGB space — tuned so the default Windows blue (#0078D4) produces a ramp that's visually in the right neighborhood of the real one. It will not byte-for-byte match Windows' own palette for arbitrary colors, but it's perceptually reasonable, monotonic, and (crucially) works for *any* input color, which a lookup table copied from the default blue would not. |
| `IAccentColorService` | interface | `CurrentAccent`, `CurrentPalette`, `InitializeAsync`, `SetAccentAsync`, `SetAccentFromImageAsync` | DI-registered (same lifetime rules as — Scoped everywhere) service that owns the app's accent color: the single base color WinUI derives its whole 7-step accent ramp from (see ), and pushes that ramp into CSS custom properties on :root so every FluentKit component that reads --accent-* updates live. Three ways to set it, matching how Windows itself offers accent color: - — explicit color, e.g. from a color picker. - — derive it from a wallpaper/photo, like Windows' "Automatically pick an accent color from my background". - Do nothing — applies (Windows' own default blue) so the app never renders with an unset accent. |
| `IThemeService` | interface | `InitializeAsync`, `Mode`, `ResolvedTheme`, `SetModeAsync` | DI-registered, not component state — so Blazor Server (one instance can serve many circuits) and MAUI Hybrid (single process, single user) both get correct, independent behavior. Register as Scoped in Server/WASM, Scoped is fine in MAUI Hybrid too (one scope per app instance). |
| `RgbColor` | struct | `B`, `Deconstruct`, `Equals`, `Equals`, `G`, `GetHashCode`, `Parse`, `PrefersDarkForeground`, `R`, `RelativeLuminance`, `ToHex`, `ToRgbTriplet`, `ToString`, `TryParse` | A plain sRGB color. Kept deliberately dumb (no HSL fields, no WinUI Windows.UI.Color dependency) so this project has zero platform coupling — works identically in WASM, Server, and MAUI Hybrid. |
| `ThemeMode` | enum | `Dark`, `Light`, `System` | The three theme modes WinUI itself exposes. "System" tracks the OS/browser preference live; Light/Dark are explicit overrides the user picked. |
| `ThemeService` | class | `DisposeAsync`, `InitializeAsync`, `Mode`, `OnSystemPreferenceChanged`, `ResolvedTheme`, `SetModeAsync` |  |
| `_Imports` | class |  |  |
