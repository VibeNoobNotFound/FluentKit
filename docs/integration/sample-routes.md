# Sample routes

The `sample` field in `api.json` points to the corresponding route in the live FluentKit
sample application:

`https://vibenoobnotfound.github.io/FluentKit{sample}`

Use the route as a visual reference, then confirm the exact parameters and events in the
package-local `api.json` before writing application code.
