# Theming and tokens

`ThemeProvider` applies light, dark, or system mode to `<html>` through `data-theme`.
Link `Tokens/tokens.css` as the public token entry point and use its variables in app CSS.
Do not link imported token implementation files directly.
